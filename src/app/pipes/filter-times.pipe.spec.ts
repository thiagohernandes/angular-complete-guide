import { FilterTimesPipe } from './filter-times.pipe';

describe('FilterTimesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTimesPipe();
    expect(pipe).toBeTruthy();
  });
});
